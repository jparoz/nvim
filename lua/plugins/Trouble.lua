-- Trouble.nvim
return { { "folke/trouble.nvim",

opts = {
    icons = false,
    padding = false,
    fold_open = "▼",
    fold_closed = "►",
    indent_lines = false,
    use_diagnostic_signs = true,
}

} }
